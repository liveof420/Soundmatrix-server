{
  "name": "soundmatrix-server",
  "version": "1.0.0",
  "description": "Servidor proxy para SoundMatrix — protege la API key de Anthropic",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
